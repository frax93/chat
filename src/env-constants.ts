// The storage key to get the user identifier
export const KEY_STORAGE_USER = 'userid';

// The storage key to get the last message of the user
export const KEY_STORAGE_LAST_MESSAGE = 'lastMessage';

// The storage key to get the messages
export const KEY_STORAGE_MESSAGES = 'messages';

// The storage key to get the users
export const KEY_STORAGE_USERS = 'user-connected';